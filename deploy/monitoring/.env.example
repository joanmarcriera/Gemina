# Copy to .env (which is git-ignored) before running the stack.
# The deploy script generates a strong random password automatically if .env
# is absent. Never commit the real .env.
GRAFANA_ADMIN_PASSWORD=change-me
