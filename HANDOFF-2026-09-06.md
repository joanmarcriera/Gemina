# Handoff — 2026-09-06 — Dependency upgrade sweep

## What changed
- Nothing. `go.mod` at the repo root declares no dependencies (`module
  github.com/joanmarcriera/gemina`, `go 1.26`, no `require` block).
- `.research-src/` holds separate vendored research clones (their own go.mod files,
  e.g. hcloud-go, wireguard-go, terraform-provider-hcloud) — out of scope for this
  sweep, not touched.

## Verified
- `go list -u -m all` shows only the module itself, nothing to bump.

## What's left
- Nothing to do until the root module gains real dependencies.
