# Handoff — 2026-09-05

**What changed:** Added `CLAUDE.md` (build/test, layout, conventions, gotchas,
pre-reading checklist). Added a "Quick Start" section to `README.md` (dev path
+ deployment path) right after the status block; rest of README untouched.

**Verified against the repo:**
- Apple team ID `D427C2J4RG` and "WS-F on-hardware verification is the only
  remaining item" — both still current per `PROJECT_STATE.md` and the latest
  commit (`c3169da`, 2026-06-28 WS-F handoff). No newer state has superseded it.
- `Makefile` targets (`bootstrap`, `test`, `lint`, `ci`, `licence-check`,
  `fetch-research`) match what's documented.
- `.claude/settings.json` hooks (`gofmt-on-edit.sh`, `redaction-guard.sh`) exist
  and match the gotchas section.
- Dual-license split (AGPL gateway / Apache-2.0 client) matches `LICENSE`/`NOTICE`.

**Left for review:** None — docs are grounded in the current repo state as of
this pass. If WS-F on-hardware verification completes, refresh the status line
in both files.
